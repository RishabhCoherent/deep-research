# Required
OPENAI_API_KEY=sk-...

# Web Search — SearXNG (primary, no API key needed)
# Start with: docker compose up -d
SEARXNG_URL=http://localhost:8888

# Optional fallback (Tavily — if SearXNG is not running)
# TAVILY_API_KEY=tvly-...
